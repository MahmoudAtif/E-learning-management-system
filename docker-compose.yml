# version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code                # Mount project for live reload
      - ./db.sqlite3:/code/db.sqlite3  # Persist SQLite DB
    env_file:
      - .env